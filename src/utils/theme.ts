import { createTheme } from "@mui/material/styles";

export const mytheme = createTheme({
  breakpoints: {
    values: {
      xs: 320,
      sm: 375,
      md: 768,
      lg: 1280,
      xl: 1920,
    },
  },
});
